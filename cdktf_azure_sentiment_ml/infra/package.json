{
  "name": "@azuretitanic/python-azure",
  "version": "0.0.1",
  "license": "MPL-2.0",
  "scripts": {
    "reinstall": "rm Pipfile.lock && PIPENV_IGNORE_VIRTUALENVS=1 pipenv install",
    "build": "cdktf get",
    "synth": "cdktf synth"
  }
}
